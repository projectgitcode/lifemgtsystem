# Utilities

This folder is reserved for reusable helper modules/utilities.

The current app logic remains consolidated in `js/app.js` to preserve the working v23 behavior. No utility code was moved out of the main app file automatically because splitting stateful functions could change runtime behavior.
